# E1: Oracle Aggregated-Distance Existence Test — Kế Hoạch Thực Hiện & Đặc Tả Giao Thức (Amended Protocol v2)

> [!IMPORTANT]
> **Trạng thái**: Amended replication under a locked pre-specified protocol (`results/e1/splits_manifest_v2.json`).
> **Đặc tả thực thi**: Phân hoạch 35 Train / 5 Val / 10 Test; Huấn luyện với early stopping (max 100 epochs, patience=15, Cosine Annealing); 9-donor wrong placebo; Estimand tính đặc hiệu trên đơn vị đô thị.

---

## Mục Tiêu Duy Nhất

Kiểm định xem **aggregated distance distribution của target city**, nếu được cung cấp ở dạng oracle, có tạo thêm giá trị cho tái tạo OD hay không — khi giữ nguyên toàn bộ backbone zero-shot đã freeze:

$$\boxed{\text{Zero-Shot Baseline } (M_0) \quad vs \quad \text{Zero-Shot} + Y_D^{GT,+}}$$

> **Oracle Existence Test**: $Y_D^{GT,+}$ được trích xuất từ ground-truth của chính target city. Mô hình **không nhận flow GT ở cấp cặp** trong quá trình adaptation, nhưng vẫn sử dụng aggregate outcome-derived của target city; do đó đây là oracle information và **không đại diện cho quan sát độc lập trong thực tế**. E1 không trả lời câu hỏi về giá trị của dữ liệu mobility thực tế (Meta, SafeGraph...).

---

## 1. Cấu Trúc Dữ Liệu & Phân Định Vai Trò Thống Kê

### 1.1 Phân Định Vai Trò: Pooled Benchmark (n=50) vs Confirmatory Sensitivity (n=40)

> [!IMPORTANT]
> **Nguyên tắc công bằng phương pháp luận (Methodological Framing & Integrity)**:
> *"We report the pooled five-fold out-of-fold benchmark across 50 cities as the primary cross-validated performance summary. Because Fold 1 contributed to protocol development, we additionally report the originally designated Folds 2–5 analysis as a confirmatory sensitivity analysis. Both analyses use five separately trained fold-specific models, and each city is evaluated exactly once when held out."*

| Analysis set | $n$ | Role | Mô tả phân tích |
|---|---:|---|---|
| **All Folds 1–5** | **50** | **Pooled out-of-fold benchmark** | Báo cáo hiệu năng tổng thể cross-validated trên toàn bộ 50 đô thị out-of-fold. |
| **Excluding Fold 1** | **40** | **Confirmatory sensitivity** | Phân tích nhạy cảm trên 40 đô thị Folds 2–5 theo chỉ định ban đầu. |
| **Fold 1** | **10** | **Development/exploratory diagnostic** | Đô thị khám phá / chẩn đoán phát hiện điểm nghẽn ban đầu. |

$$\boxed{\text{Primary Out-of-Fold Benchmark: } n = 50 \text{ Cities (All Folds 1–5)}}$$

$$\boxed{\text{Confirmatory Sensitivity Analysis: } n = 40 \text{ Cities (Folds 2–5)}}$$

---

## 2. Tham Số Cố Định Cho E1-v2

| Tham số | Giá trị | Lý do |
|---|---|---|
| **$K_{\text{move}}$ bins** | **8 moving-distance bins** | Pre-specified; Bin 0 (intrazonal, $D=0$) excluded |
| **$q$** | **1.0 (within-tolerance calibration)** | Closed-form, mass-preservation error $< 10^{-5}$ & bin-matching error $< 10^{-5}$ |
| **Bin edge scheme** | **Pair-weighted quantile từ 35 train cities mỗi fold** | Tránh leakage; consistent giữa target và wrong donor; strict $K_{\text{active}}=8$ |
| **Wrong donor placebo** | **Toàn bộ 9 wrong donors còn lại trong test fold** | $\bar{\Delta}_c^{\text{wrong}} = \frac{1}{9}\sum_{d \neq c}\Delta_{c,d}^{\text{wrong}}$, giảm phương sai chọn donor tùy ý |
| **Primary specificity estimand** | **$\Delta_c^{\text{specificity}} = \Delta_c^{\text{target}} - \bar{\Delta}_c^{\text{wrong}}$** | Cell-level OD gain vượt trên placebo, đơn vị quan sát là đô thị |
| **Evaluation domain** | **Interzonal $\Omega_c^+$: $i \neq j, D_{ij} > 0$** | Loại intrazonal khỏi CPC |
| **Training & Model Selection** | **35 train / 5 val / 10 test per fold** | Val interzonal CPC chọn best epoch (patience=15, max 100 epochs, cosine annealing) |
| **Validation Selection** | **5-stratum size stratification (seed 20260818)** | 1 thành phố mỗi stratum quy mô; khóa tại `splits_manifest_v2.json` |
| **Outer Test Folds** | **Khóa nguyên vẹn 100% từ E1-v1** | Tránh post-hoc selection / tie-breaking perturbation |
| **Statistical unit** | **City ($n=50$ benchmark, $n=40$ confirmatory)** | Fold-stratified bootstrap (ddof=1) & Paired Wilcoxon trên $\Delta_c^{\text{specificity}}$ |

---

## 3. Đặc Tả Bin Edges $K_{\text{move}} = 8$

Tính từ **35 train cities** của mỗi fold. Pair-weighted quantile (thành phố lớn đóng góp nhiều pairs hơn — chủ ý, ghi rõ trong paper).

```python
def compute_kbin_edges(train_cities, K=8, data_root="data"):
    """Pair-weighted quantile edges from 35 training cities.
    Intrazonal (D=0) excluded. Returns (K+1,) edges including 0.0 and inf."""
    all_dist = []
    for city_name in train_cities:
        cd = load_city(city_name, data_root=data_root)
        dist_km = np.expm1(cd.pair_distance.numpy())
        inter = (cd.pair_o_idx.numpy() != cd.pair_d_idx.numpy()) & (dist_km > 0.0)
        all_dist.extend(dist_km[inter].tolist())
    all_dist = np.array(all_dist)

    # K-1 internal quantile breakpoints → K bins
    quantiles = np.linspace(0, 100, K + 1)[1:-1]
    internal_edges = np.percentile(all_dist, quantiles)

    # Deduplicate: remove duplicate edges (report actual K_active < K if occurs)
    internal_edges = np.unique(internal_edges)
    edges = np.concatenate([[0.0], internal_edges, [np.inf]])

    # INVARIANT: edges strictly increasing
    assert np.all(np.diff(edges) > 0), f"Non-strict edges: {edges}"

    K_active = len(edges) - 1
    if K_active < K:
        print(f"WARNING: Only {K_active}/{K} active bins (quantile degeneration)")

    return edges, K_active
```

---

## 4. Pipeline Chi Tiết Từng Fold (E1-v2)

### Bước 1 — Train Backbone với Validation Early Stopping

```python
model_f, scaler_f, train_info = train_zero_shot_model(
    train_city_names=train35_cities[f],
    val_city_names=val5_cities[f],
    data_root="data",
    epochs=100,
    patience=15,
    min_delta=1e-4,
    device_str="cpu",
    return_info=True,
    seed=42 + f,
)
# → θ^(f) frozen tại best epoch, model_f.eval(), requires_grad=False
```

### Bước 2 — Compute Bin Edges từ 35 Train Cities

```python
bin_edges_f, K_active_f = compute_kbin_edges(train35_cities[f], K=8, data_root="data")
# Strict invariant: K_active == 8
```

### Bước 3 — Với Từng Test City $c \in \text{Test}_f$ (10 Cities)

#### A. Zero-Shot Baseline ($M_0$)

```python
cd = load_city(c, data_root="data", feature_scaler=scaler_f)
edge_idx, edge_dist = build_radius_graph(cd.lon_lat.numpy(), radius_km=5.0)

with torch.no_grad():
    T0 = model_f(
        cd.node_features, edge_idx, edge_dist,
        cd.pair_o_idx, cd.pair_d_idx,
        cd.pair_distance, cd.population,
        return_conditional_mean=True,
    )

dist_km = np.expm1(cd.pair_distance.numpy())
inter_mask = (cd.pair_o_idx.numpy() != cd.pair_d_idx.numpy()) & (dist_km > 0.0)
t_gt = cd.pair_trips.numpy()

CPC_0 = compute_cpc_pair(t_gt[inter_mask], T0.numpy()[inter_mask])
```

#### B. Extract Oracle $Y_{D,c}^{GT,+}$ — Vector $K_{\text{move}}=8$ Chiều

```python
def extract_yd_kbins(dist_km, trips, bin_edges, inter_mask):
    """Extract K-bin oracle trip-length distribution.
    Aggregates GT flows by distance bin — NOT pair-level flows.
    """
    K = len(bin_edges) - 1
    yd = np.zeros(K)
    inter_trips = trips[inter_mask]
    inter_dist = dist_km[inter_mask]
    for k in range(K):
        lo, hi = bin_edges[k], bin_edges[k + 1]
        in_bin = (inter_dist > lo) & (inter_dist <= hi)
        yd[k] = inter_trips[in_bin].sum()
    total = yd.sum()
    return yd / total if total > 0 else yd  # shape: (K,) summing to 1.0

Y_D_target = extract_yd_kbins(dist_km, t_gt, bin_edges_f, inter_mask)
```

#### C. Treatment Adaptation ($+Y_D^{\text{target}}$) — Closed-Form, $q=1.0$ within-tolerance

```python
def calibrate_kbins(T0_np, dist_km, inter_mask, Y_D, bin_edges, q=1.0, tolerance=1e-5):
    """Closed-form K-bin Moving-Bin calibration.
    Invariants:
      1. Interzonal mass preserved: |sum(T_cal[inter]) - sum(T0[inter])| < tolerance * sum(T0[inter])
      2. Intrazonal identity: T_cal[~inter] == T0[~inter]
      3. At q=1: bin proportions match Y_D_cond within tolerance for active bins
    """
    K = len(bin_edges) - 1
    T_cal = T0_np.copy()
    inter_T0 = T0_np[inter_mask]
    N_hat = inter_T0.sum()
    if N_hat <= 0:
        return T_cal

    inter_dist = dist_km[inter_mask]
    Y_hat = np.zeros(K)
    active = np.zeros(K, dtype=bool)
    for k in range(K):
        in_bin = (inter_dist > bin_edges[k]) & (inter_dist <= bin_edges[k + 1])
        Y_hat[k] = inter_T0[in_bin].sum() / N_hat
        active[k] = in_bin.any()

    Y_D_active = Y_D * active
    denom = Y_D_active.sum()
    Y_D_cond = Y_D_active / denom if denom > 0 else Y_hat.copy()

    w = np.ones(K)
    for k in range(K):
        if active[k] and Y_hat[k] > 0:
            w[k] = (Y_D_cond[k] / Y_hat[k]) ** q

    weighted_mass = (Y_hat * w).sum()
    s = w / weighted_mass if weighted_mass > 0 else np.ones(K)

    idx = np.where(inter_mask)[0]
    for k in range(K):
        in_bin = (inter_dist > bin_edges[k]) & (inter_dist <= bin_edges[k + 1])
        T_cal[idx[in_bin]] = T0_np[idx[in_bin]] * s[k]

    return T_cal

T_YD = calibrate_kbins(T0.numpy(), dist_km, inter_mask, Y_D_target, bin_edges_f, q=1.0)
CPC_YD = compute_cpc_pair(t_gt[inter_mask], T_YD[inter_mask])
delta_target = CPC_YD - CPC_0
```

#### D. Multi-Donor Placebo Control (9 Wrong Donors in Test Fold)

```python
wrong_donors = [d for d in sorted(test_cities_f) if d != c]
assert len(wrong_donors) == 9

delta_wrong_list = []
for donor in wrong_donors:
    cd_donor = load_city(donor, data_root="data", feature_scaler=scaler_f)
    dist_km_d = np.expm1(cd_donor.pair_distance.numpy())
    inter_d = (cd_donor.pair_o_idx.numpy() != cd_donor.pair_d_idx.numpy()) & (dist_km_d > 0.0)
    Y_D_wrong = extract_yd_kbins(dist_km_d, cd_donor.pair_trips.numpy(), bin_edges_f, inter_d)

    T_wrong_d = calibrate_kbins(T0.numpy(), dist_km, inter_mask, Y_D_wrong, bin_edges_f, q=1.0)
    cpc_wrong_d = compute_cpc_pair(t_gt[inter_mask], T_wrong_d[inter_mask])
    delta_wrong_list.append(cpc_wrong_d - CPC_0)

delta_wrong_avg9 = float(np.mean(delta_wrong_list))
delta_specificity = float(delta_target - delta_wrong_avg9)
```

---

## 5. Invariants Phải Verify Trước Khi Khoá Kết Quả

- [ ] **Mass preservation**: $|\sum T^{(YD)} - \sum T^{(0)}|_{\Omega_c^+} < 10^{-5} \cdot \sum T^{(0)}$
- [ ] **Intrazonal identity**: $T_{ii}^{(YD)} = T_{ii}^{(0)}$ với atol $< 10^{-6}$
- [ ] **Bin matching tại $q=1$ (within-tolerance)**: $|\hat{y}_k^{(YD)} - Y_{D,k}| < 10^{-5}$ trên bins active
- [ ] **GT independence**: Hoán vị $T^{GT}$ không thay đổi $T^{(YD)}$ (T54 đã PASS)
- [ ] **Multi-donor distinctness**: Đúng 9 wrong donors trong fold, $d \neq c$ với mọi test city
- [ ] **Bin edges strictly increasing**: `np.all(np.diff(edges) > 0)` per fold
- [ ] **K_active per fold reported**: Khóa cứng $K_{\text{active}} = 8$
- [ ] **Manifest integrity**: Khớp mã băm SHA-256 của `results/e1/splits_manifest_v2.json`

---

## 6. Aggregation, Specificity Estimand & Thống Kê

```python
delta_target = np.array([r["delta_cpc_target"]      for r in results])
delta_wrong  = np.array([r["delta_cpc_wrong"]       for r in results])
delta_spec   = np.array([r["delta_cpc_specificity"] for r in results])
fold_ids     = np.array([r["fold"]                  for r in results])

# Fold-stratified bootstrap CI
# (cities within same fold share one backbone → within-fold correlated)
rng = np.random.default_rng(42)
boot_spec = []
for _ in range(10_000):
    samp = []
    for f in sorted(set(fold_ids)):
        fd = delta_spec[fold_ids == f]
        if len(fd) > 0:
            samp.extend(rng.choice(fd, size=len(fd), replace=True))
    boot_spec.append(np.mean(samp))
ci_l_spec, ci_h_spec = np.percentile(boot_spec, [2.5, 97.5])

# Wilcoxon signed-rank trên specificity (city-level, non-parametric)
_, p_specificity = stats.wilcoxon(delta_spec, alternative="greater")
```

### Acceptance Criteria Cho Confirmatory Folds 2–5 ($n=40$)

| Criterion | Điều kiện yêu cầu | Cách đọc |
|---|---|---|
| **$\overline{\Delta\text{CPC}}^{\text{target}} > 0$** | Mean $\Delta\text{CPC}^{\text{target}} > 0$ | Oracle signal tồn tại |
| **95% CI lower bound $> 0$** | $\text{CI}_{\text{lower}} > 0$ | Confirmatory evidence của marginal gain |
| **Specificity Superiority** | $\overline{\Delta\text{CPC}}^{\text{target}} > \overline{\Delta\text{CPC}}^{\text{wrong}}$ | Target-specific signal vượt trội generic regularization |
| **Specificity CI lower bound $> 0$** | Specificity $\text{CI}_{\text{lower}} > 0$ | Gain thuần túy của thông tin đích mang tính dương chắc chắn |
| **Specificity Significance** | Paired Wilcoxon $p < 0.05$ | Kiểm định phi tham số cặp đạt ý nghĩa thống kê |
| **City-level Specificity Consistency** | $n_+ / 40 > 28$ ($> 70\%$) | Tính nhất quán trên đa số đô thị confirmatory |

---

## 7. Các Giới Hạn Khoa Học & Caveats Bắt Buộc

> [!CAUTION]
> 1. **Bản chất Oracle Aggregate (Outcome-Derived)**:
>    - $Y_D^{\text{target}}$ được tạo từ chính ground truth trips của đô thị đích qua phép gộp 8 bins khoảng cách.
>    - E1 kiểm định *frozen zero-shot model + oracle target-city aggregate constraint*, không đại diện cho quan sát di chuyển độc lập từ dữ liệu viễn thông thực tế.
> 2. **Phân Tích Hiệu Ứng Placebo (Generic Regularization vs Target Specificity)**:
>    - Hiệu ứng wrong donor đại diện cho **generic distance-structure regularization**.
>    - Hiệu ứng target-specific được đo lường chính xác bằng estimand $\Delta_c^{\text{specificity}} = \Delta_c^{\text{target}} - \overline{\Delta}_c^{\text{wrong}}$.
> 3. **Tính Nhất Quán**:
>    - Đánh giá trên 40 confirmatory cities (Folds 2–5) kết hợp kiểm định nhạy cảm, không giả định hiệu ứng là hằng số cho mọi hình thái đô thị.

---

## 8. Trạng Thái Hoàn Thành

```
[✓] Manifest v2: Locked at results/e1/splits_manifest_v2.json (SHA-256 verified)
[✓] city_splits.py: 35/5/10 splits, size-stratified validation, locked v1 test sets
[✓] yd_extractor.py: compute_kbin_edges() from 35 train cities + extract_yd_kbins()
[✓] bin_calibration.py: calibrate_kbins() (q=1.0 within-tolerance 1e-5)
[✓] run_e1.py: Protocol v2, early stopping (100 epochs, patience=15), 9 wrong donors
[✓] test_e1_contracts.py: 10/10 test contracts PASSED (100%)
[✓] Markdown tables & JSON schemas updated to 3-level analysis hierarchy (50 / 40 / 10)
```
